# Fonts

Primary sans-serif is **Work Sans**, loaded from Google Fonts in `src/components/CommonHead.astro`. Mono is **Geist Mono** (same).

Use this folder for any self-hosted font files you add later.
