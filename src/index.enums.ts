export enum FigureType {
  VideoLoop = 'video-loop',
  Image = 'image',
}

export enum LetterboxdFeedType {
  Watch = 'letterboxd-watch',
  List = 'letterboxd-list',
  Review = 'letterboxd-review',
}
